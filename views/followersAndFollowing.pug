extends layouts/main-layout.pug

block content 
  .profile-tabs
    +createTab('Followers',`/profile/${userId}/followers`,true)
    +createTab('Followings',`/profile/${userId}/followings`,false)
  .post-wrapper 
    +createPostModals(userInfo)
block scripts 
    