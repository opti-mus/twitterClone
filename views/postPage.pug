extends layouts/main-layout.pug

block content 
    script. 
        var postId = '!{postId}'
    .post-wrapper 
    +createPostModals(userInfo)
block scripts 
    script(src="/scripts/postPage.js") 